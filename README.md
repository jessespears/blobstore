blobstore is an app to store and retrieve binary blobs. It also wraps `fortune`.

## Prerequisites

- [minikube](https://minikube.sigs.k8s.io/docs/start/)

## Running

```
make start   # start minikube cluster
make stop    # stop cluster (preserves state)
make delete  # destroy cluster
```
